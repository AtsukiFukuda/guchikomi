<div class= "container">
<h2>投稿詳細</h2>
  <div class="post">
    <p>企業名：<%= @post.company_name %>(<%= @post.category %>)</p>
    <p>勤務地：<%= @post.work_location %></p>
    <p>年収：<%= @post.salary %>万円</p>
    <p>満足度(5点満点)：<%= @post.overall %></p>
    <hr>
    <p><div class="title"><%= @post.title %></div></p>
    <p><%= @post.body %></p><br>
    <p>投稿日時：<%= @post.created_at %></p>
    <p>共感 
    <% if current_user.already_liked?(@post) %>
      <%= link_to post_like_path(@post), method: :delete do %>
        <span class= 'fa fa-thumbs-up like-btn-unlike'>
        </span>
      <% end %>
    <% else %>
      <%= link_to post_likes_path(@post), method: :post do %>
        <span class= 'fa fa-thumbs-up like-btn'>
        </span>
      <% end %>
    <% end %><%= @post.likes.count %>人</p>
    (<% @post.liked_users.each do |user| %>
      <%= link_to(user.name, user_path(user.id), class: "link") %>さん
    <% end %>)
    <br><br>
    <p>頑張って
    <% if current_user.already_cheered?(@post) %>
      <%= link_to post_cheer_path(@post), method: :delete do %>
        <span class= 'fa fa-heart like-btn-unlike'>
        </span>
      <% end %>
    <% else %>
      <%= link_to post_cheers_path(@post), method: :post do %>
        <span class= 'fa fa-heart like-btn'>
        </span>
      <% end %>
    <% end %><%= @post.cheers.count %>人</p>
    
    (<% @post.cheered_users.each do |user| %>
      <%= link_to(user.name, user_path(user.id), class: "link") %>さん
    <% end %>)
    <br><br>
    <p>飲んで忘れよ
    <% if current_user.already_forgot?(@post) %>
      <%= link_to post_forget_path(@post), method: :delete do %>
        <span class= 'fa fa-beer like-btn-unlike'>
        </span>
      <% end %>
    <% else %>
      <%= link_to post_forgets_path(@post), method: :post do %>
        <span class= 'fa fa-beer like-btn'>
        </span>
      <% end %>
    <% end %><%= @post.forgets.count %>人</p>
    
    (<% @post.forgot_users.each do |user| %>
      <%= link_to(user.name, user_path(user.id), class: "link") %>さん
    <% end %>)
    <br>
    <% if @post.user_id == current_user.id %><br>
      <%= link_to "編集する", edit_post_path %>
      <%= link_to "削除する", post_path(@post.id), method: :delete %>
    <% end %>
  </div><br>
  <%= link_to "投稿一覧に戻る", posts_path, class: "btn btn-primary" %>
  <br><br><br>
</div>

<div class=ESBI>
  <p1>E</p1>
  <p2>S</p2>
  <p3>B</p3>
  <p4>I</p4>
</div>

